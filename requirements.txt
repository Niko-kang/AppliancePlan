# Convenience meta-file. Prefer split installs:
#   pip install -r requirements-train.txt
#   pip install -r requirements-infer.txt
#
# Installing train + vLLM in one env may conflict; use two conda envs when in doubt.

-r requirements-train.txt
-r requirements-infer.txt
